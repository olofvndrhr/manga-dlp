[tox]
envlist = py38, py39, py310
isolated_build = True

[testenv]
deps =
    -rrequirements_dev.txt

commands =
    pytest -x --basetemp="{envtmpdir}" {posargs}

[testenv:coverage]
deps =
    -rrequirements_dev.txt

commands =
    coverage erase
    coverage run
    coverage xml -i
